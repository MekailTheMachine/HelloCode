#include <iostream> //iostream allows us to use
using namespace std;
 
int main() {
   // std::cout << "Hello Cpp ! "; we are using the standard name space in line 2 so we can use the line below instead.
   cout << "Hello Cpp ! ";
   // now our main function is an integer and we will return it with the value 0 to wrap things up there.
    return 0;
}